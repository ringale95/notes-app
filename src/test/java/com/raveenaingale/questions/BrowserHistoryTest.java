package com.raveenaingale.questions;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class BrowserHistoryTest {

    @Test
    public void testBrowserHistoryOperations() {
//        BrowserHistory browser = new BrowserHistory();
//
//        assertEquals("No pages visited yet", browser.getCurrentPage());
//
//        browser.visit("https://www.example.com");
//        assertEquals("https://www.example.com", browser.getCurrentPage());
//
//        browser.visit("https://www.example.com/page1");
//        assertEquals("https://www.example.com/page1", browser.getCurrentPage());
//
//        assertEquals("https://www.example.com", browser.back());
//        assertEquals("https://www.example.com", browser.getCurrentPage());
//
//        assertEquals("No previous page", browser.back());
//        assertEquals("https://www.example.com", browser.getCurrentPage());
    }
}

